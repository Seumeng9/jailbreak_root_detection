pluginManagement {
    repositories {
        gradlePluginPortal()
        google()
        mavenCentral()
    }
    plugins {
        id 'com.android.library' version "${AGPVersion}"
    }
}

rootProject.name = 'jailbreak_root_detection'
